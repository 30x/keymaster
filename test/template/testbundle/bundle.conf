upstream target {
  server mocktarget.apigee.net;
}

server {
  server_name myhost;
  listen 9000;

  location / {
    {{.Pipe "dump"}}
    proxy_pass http://target;
  }

  location /iloveapis {
    {{.Pipe "apikey"}}
    proxy_pass http://target;
  }
}
