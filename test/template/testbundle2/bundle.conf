upstream target {
  server mocktarget.apigee.net;
}

server {
  server_name myhost2;
  listen 9000;

  location / {
    {{.Pipe "dump"}}
    proxy_pass http://target;
  }

  location /iloveapis {
    {{.Pipe "apikey"}}
    proxy_pass http://target;
  }
}
